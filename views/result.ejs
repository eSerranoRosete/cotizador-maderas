<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Resultado de cotización Ciudad Maderas terrenos premium">
    <title>Detalles de la Cotización</title>
    <link rel="stylesheet" href="/static/css/output.css">
</head>
<body>
    <div class="text-gray-900 p-3 sm:p-6 flex flex-col m-auto max-w-xl text-xs sm:text-base relative">

        <p class="absolute text-4xl text-center transform top-2/3 inset-x-0 -rotate-45 font-black -z-10 text-slate-700/20"><%= query.title %></p>

        <img src="/static/images/logo-color.svg" alt="Ciudad Maderas Logo" class="w-64 m-auto my-5">
        <h1 class="text-lg sm:text-2xl m-auto font-semibold tracking-wide"><%= query.title %> </h1>
        <h2 class="text-md sm:text-lg mb-10 m-auto"><%= query.plaza %> </h2>
        <h5 class="text-lg sm:text-2xl font-semibold m-auto">Detalles de la cotización</h5>
        <p class="text-md sm:text-sm mb-10 m-auto">Los precios se muestran en pesos mexicanos.</p>

        <p class="font-semibold">Desarrollo: </p>
        <p class="text-gray-700 mb-5" id="location_r"><%- query.location %></p>
        <table>
            <tr>
                <td class="font-semibold">Fecha:</td>
                <td id="date_r" class="text-gray-700 text-right"><%- query.date %></td>
            </tr>
            <tr>
                <td class="font-semibold">Cluster:</td>
                <td id="cluster_r" class="text-gray-700 text-right"><%- query.cluster %></td>
            </tr>
            <tr>
                <td class="font-semibold">Lote:</td>
                <td id="landSection_r" class="text-gray-700 text-right"><%- query.landSection %></td>
            </tr>
            <tr>
                <td class="font-semibold">Superficie:</td>
                <td id="squareMeters_r" class="text-gray-700 text-right"><%- query.squareMeters + " m2" %></td>
            </tr>
            <tr>
                <td class="font-semibold">Plazo de adquisición:</td>
                <td id="squareMeters_r" class="text-gray-700 text-right"><%- query.term + " años" %></td>
            </tr>
            <tr>
                <td class="font-semibold">Precio de lista m2:</td>
                <td id="landPrice_r" class="text-gray-700 text-right"><%- result.landPricem2 %> </td>
            </tr>
            <tr>
                <td class="font-semibold">Valor del terreno:</td>
                <td id="initialLandPrice_r" class="text-gray-700 text-right"><%- result.lockedLandPrice %> </td>
            </tr>
        </table>

        <hr class="my-5">

        <% if (query.baseDiscount > 0 || query.additionalDiscount > 0 || query.extraDiscounts > 0) { %>
         
            <table>

                <% if (query.baseDiscount > 0) { %>
                    <tr>
                        <td id="baseDiscount_l" class="font-semibold">Descuento Base:</td>
                        <td id="baseDiscount_r" class="text-gray-700 text-right"><%- query.baseDiscount + "%" %> </td>
                    </tr>
                <% } %>   
                <% if (query.additionalDiscount > 0) { %>
                    <tr>
                        <td id="additionalDiscount_l" class="font-semibold">Descuento adicional:</td>
                        <td id="additionalDiscount_r" class="text-gray-700 text-right"><%- query.additionalDiscount + "%" %> </td>
                    </tr>
                <% } %>
                <% if (query.extraDiscounts > 0) { %>
                    <tr>
                        <td id="extraDiscounts_l" class="font-semibold">Bonos y promociones:</td>
                        <td id="extraDiscounts_r" class="text-gray-700 text-right"><%= result.extraDiscounts %> </td>
                    </tr>
                <% } %>
            </table>

            <hr class="my-5">
        <% } %>

        <table>
            <tr>
                <td class="font-semibold">Porcentaje de Enganche:</td>
                <td id="downPaymentPercentage" class="text-gray-700 text-right"><%- query.downPayment + "%" %> </td>
            </tr>

            <% if (query.fastPayDiscount == "true") { %>
                <tr>
                    <td id="fastPay_l" class="font-semibold">Descuento Adicional sobre Enganche:</td>
                    <td id="fastPay_r" class="text-gray-700 text-right">10%</td>
                </tr>
            <% } %>

            <tr>
                <td id="downPayment_l" class="font-semibold">Total de enganche:</td>
                <td id="downPayment_r" class="text-gray-700 text-right"><%- result.downPayment %> </td>
            </tr>
        </table>

        <hr class="my-5">

        <% if (query.baseDiscount > 0 || query.additionalDiscount > 0 || query.extraDiscounts > 0 || query.fastPayDiscount == "true") { %>

            <table>
                <tr>
                    <td id="savings_l" class="font-semibold">Ahorro total por descuentos y promociones:</td>
                    <td id="savings_r" class="text-gray-700 text-right"><%= result.savings %> </td>
                </tr>
                <tr>
                    <td id="savings_l" class="font-semibold">Precio final del terreno:</td>
                    <td id="savings_r" class="text-gray-700 text-right"><%= result.finalLandPrice %> </td>
                </tr>
                <tr>
                    <td id="savings_l" class="font-semibold">Precio final m2:</td>
                    <td id="savings_r" class="text-gray-700 text-right"><%= result.finalSquareMeterPrice %> </td>
                </tr>
            </table>
            <hr class="my-5">
        <% } %>

        <table>

            <% if (result.period1.term > 0) { %>
                <tr>
                    <td id="period1Term_r" class="font-semibold"><%- result.period1.term %> Mensualidades de:</td>
                    <td id="period1Amount_r" class="text-gray-700 text-right"><%- result.period1.amount %> </td>
                </tr>
            <% } %>

            <% if (result.period2.term > 0) { %>
                <tr>
                    <td id="period2Term_r" class="font-semibold"><%- result.period2.term %> Mensualidades con 1% de interés de:</td>
                    <td id="period2Amount_r" class="text-gray-700 text-right"><%- result.period2.amount %> </td>
                </tr>
            <% } %>

            <% if (result.period3.term > 0) { %>
                <tr>
                    <td id="period3Term_r" class="font-semibold"><%- result.period3.term %> Mensualidades con 1.25% de interés de:</td>
                    <td id="period3Amount_r" class="text-gray-700 text-right"><%- result.period3.amount %> </td>
                </tr>
            <% } %>

        </table>
        <p class="mt-10 text-xs sm:text-sm m-auto">*Vigencia de la cotización 10 días*</p>
        <a href="javascript:history.back()" class="bg-slate-200 text-gray-700 text-center block p-2 rounded-xl text-sm cursor-pointer mt-3">Volver al cotizador</a>
    </div>
</body>
</html>